<h1>Users</h1>

<table class="table table-striped">
  <thead>
  <tr>
    <th>Nimi</th>
    <th>Admin?</th>
  </tr>
  </thead>

  <tbody>
    <% @users.each do |user| %>
      <tr>
        <td><%= link_to user.username, user %></td>
        <td><%= user.admin? %></td>
      </tr>
    <% end %>
  </tbody>
</table>