<h1>
  <%= @course.name %>
  <small>
    <% if @course.registeration_open? %>
        <span class="label label-success">Registeration open</span>
    <% end %>

    <% if not @course.active? %>
        <span class="label label-danger">Completed</span>
    <% end %>
  </small>
</h1>

<% if current_user.is_leader? @course or current_user.is_teacher? @course %>
    <div class="tabs">
      <ul class="nav nav-tabs">
        <li <%= 'class="active"'.html_safe if current_page? action: 'show' %>>
          <a href="<%= course_path @course %>">
            <span class="glyphicon glyphicon-home"></span> Main
          </a>
        </li>

        <li <%= 'class="active"'.html_safe if current_page? action: 'weeklysubmissions' %>>
          <a href="<%= weeklysubmissions_course_path @course %>">
            <span class="glyphicon glyphicon-check"></span> Weekly submissions
          </a>
        </li>

        <li <%= 'class="active"'.html_safe if current_page? action: 'codereviews' %>>
          <a href="<%= codereviews_course_path @course %>">
            <span class="glyphicon glyphicon-eye-open"></span> Code reviews
          </a>
        </li>

        <li <%= 'class="active"'.html_safe if current_page? action: 'edit' %>>
          <a href="<%= edit_course_path @course %>">
            <span class="glyphicon glyphicon-cog"></span> Settings
          </a>
        </li>
      </ul>
    </div>
<% end %>