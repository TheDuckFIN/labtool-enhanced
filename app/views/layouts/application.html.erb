<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "Labtool Enhanced" %></title>
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "Labtool Enhanced" %>">
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track' => true %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>
  </head>
  <body>
    <header>
      <%= render 'layouts/navigation' %>
    </header>
    <main role="main">
      <noscript><div class="alert alert-danger" style="margin-top: 12px">This page uses javascript. Please enable javascript for full labtool experience!</div></noscript>
      <%= render 'layouts/messages' %>
      <%= yield %>
    </main>
  </body>
</html>
